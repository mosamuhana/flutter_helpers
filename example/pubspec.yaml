name: example
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.14.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.4
  listenable_extensions:
    path: ../
  rxdart: ^0.27.2
  provider: ^6.0.1
  flutter_disposables:
    git: https://github.com/mosamuhana/flutter_disposables

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.4

flutter:
  uses-material-design: true
